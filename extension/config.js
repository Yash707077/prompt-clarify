// config.js — where the extension sends its requests.
//
// After deploying the worker (see worker/README.md), paste its URL here.
// Until then, leave it as null: the extension falls back to asking each user
// for their own API key, which is exactly how it behaved before the proxy.

const PROXY_URL = "https://prompt-clarify-api.yashraj707077.workers.dev";
